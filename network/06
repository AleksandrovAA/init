#!/bin/bash
# Файл конфигурации распознавателя содержит информацию, которая читается подпрограммами распознавателя при первом запуске процесса. Файл предназначен для человека
# cat файл, видим nameserver 192.168.50.2 - это и есть мой dns сервер
cat /etc/resolv.conf
