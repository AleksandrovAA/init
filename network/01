#!/bin/bash
# cut -d "разделитель" -f 1 - вырезает 1 кусок перед разделителем
# Имена сетевых устройств без информации
ifconfig | grep "mtu" | cut -d ":" -f 1

# Вариант выводит результат в 1 строку
ifconfig -l
