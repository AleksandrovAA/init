#!/bin/bash

#Редактирование файла с использованием текстового редактора nano или vi
sudo nano /etc/hostname
#В этом файле также надо заменить имя хоста
sudo nano /etc/hosts
#Перезагрузить для сохранения результатов
sudo reboot
